{
  "name": "seismic-viem-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rimraf dist",
    "typecheck": "tsc --noEmit",
    "test:anvil": "CHAIN=anvil bun test sandbox.test.ts",
    "test:devnet": "CHAIN=devnet bun test contract.test.ts"
  },
  "devDependencies": {
    "dotenv": "^16.4.7",
    "terminate": "^2.8.0"
  }
}
