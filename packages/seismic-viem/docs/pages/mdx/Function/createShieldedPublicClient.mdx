---
custom_edit_url: null
---

# createShieldedPublicClient
<div className="doc-side">
<div className="doc-main-source">

[source](https://github.com/SeismicSystems/seismic-client/blob/86b3230/packages/seismic-viem/src/client.ts#L200)

</div>
</div>

<div className="doc-subtitle">

Creates a shielded public client with extended functionality for interacting
with shielded blockchain features.

This function builds upon the base public client and extends it with shielded-specific actions,
such as retrieving the Trusted Execution Environment (TEE) public key and interacting with
shielded features.

</div>

```ts
createShieldedPublicClient<transport,chain,rpcSchema>(parameters: { batch?: { multicall?: boolean | ({ batchSize?: number | undefined; wait?: number | undefined; }) }; cacheTime?: number; ccipRead?: false | { request?: ((parameters: CcipRequestParameters) => Promise<`0x"${string}`>) }; chain?: Chain | chain; key?: string; name?: string; pollingInterval?: number; rpcSchema?: rpcSchema; transport: transport }): ShieldedPublicClient<transport, chain, undefined, rpcSchema>
```

<h4>Parameters</h4>

<div className="doc-param">

<div className="doc-param-header">

<span className="doc-name">parameters</span>

<div className="doc-type">

{&nbsp;batch?:&nbsp;{&nbsp;multicall?:&nbsp;boolean&nbsp;|&nbsp;({&nbsp;batchSize?:&nbsp;number&nbsp;|&nbsp;undefined;&nbsp;wait?:&nbsp;number&nbsp;|&nbsp;undefined;&nbsp;})&nbsp;};&nbsp;cacheTime?:&nbsp;number;&nbsp;ccipRead?:&nbsp;false&nbsp;|&nbsp;{&nbsp;request?:&nbsp;((parameters:&nbsp;CcipRequestParameters)&nbsp;=&gt;&nbsp;Promise&lt;`0x&quot;${string}`&gt;)&nbsp;};&nbsp;chain?:&nbsp;Chain&nbsp;|&nbsp;chain;&nbsp;key?:&nbsp;string;&nbsp;name?:&nbsp;string;&nbsp;pollingInterval?:&nbsp;number;&nbsp;rpcSchema?:&nbsp;rpcSchema;&nbsp;transport:&nbsp;transport&nbsp;}

</div>

</div>

<div className="doc-desc">

The configuration parameters for creating the public client. Includes transport,
chain, and optional RPC schema definitions.

</div>

<div className="doc-sub-param">

<span className="doc-name">parameters.batch</span>

<div className="doc-type">

Object

</div>

<div className="doc-desc">

Flags for batch settings.

</div>

</div>

<div className="doc-sub-param">

<span className="doc-name">parameters.cacheTime</span>

<div className="doc-type">

number

</div>

<div className="doc-desc">

Time (in ms) that cached data will remain in memory.

</div>

</div>

<div className="doc-sub-param">

<span className="doc-name">parameters.ccipRead</span>

<div className="doc-type">

false&nbsp;|&nbsp;Object

</div>

<div className="doc-desc">

[CCIP Read](https://eips.ethereum.org/EIPS/eip-3668) configuration.
If `false`, the client will not support offchain CCIP lookups.

</div>

</div>

<div className="doc-sub-param">

<span className="doc-name">parameters.chain</span>

<div className="doc-type">

Chain&nbsp;|&nbsp;chain

</div>

<div className="doc-desc">

Chain for the client.

</div>

</div>

<div className="doc-sub-param">

<span className="doc-name">parameters.key</span>

<div className="doc-type">

string

</div>

<div className="doc-desc">

A key for the client.

</div>

</div>

<div className="doc-sub-param">

<span className="doc-name">parameters.name</span>

<div className="doc-type">

string

</div>

<div className="doc-desc">

A name for the client.

</div>

</div>

<div className="doc-sub-param">

<span className="doc-name">parameters.pollingInterval</span>

<div className="doc-type">

number

</div>

<div className="doc-desc">

Frequency (in ms) for polling enabled actions & events.

</div>

</div>

<div className="doc-sub-param">

<span className="doc-name">parameters.rpcSchema</span>

<div className="doc-type">

rpcSchema

</div>

<div className="doc-desc">

Typed JSON-RPC schema for the client.

</div>

</div>

<div className="doc-sub-param">

<span className="doc-name">parameters.transport</span>

<div className="doc-type">

transport

</div>

<div className="doc-desc">

The RPC transport

</div>

</div>

</div>

<h4 className="doc-comment-title">Returns</h4>

A shielded public client
instance with extended shielded actions.
:::info Example

```typescript
const client = createShieldedPublicClient({
  transport: httpTransport,
  chain: seismicChain,
  rpcSchema: customRpcSchema,
});

// Use shielded public actions
const teePublicKey = await client.getTeePublicKey();
console.log('TEE Public Key:', teePublicKey);
```

:::
<h4 className="doc-comment-title">Remarks</h4>

The returned client includes all standard public client actions and additional
shielded-specific actions. It is suitable for applications requiring secure and
shielded interactions with blockchain networks.