name: CI

on:
  push:
    branches: ['main']
  pull_request:
    branches: ['main']

jobs:
  lint:
    runs-on: ubuntu-24.04
    steps:
      - name: Check out repo
        uses: actions/checkout@v3
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.1.42
      - name: Install dependencies
        run: bun install
      - name: Check lint
        run: bun run lint:check

  typecheck:
    runs-on: ubuntu-24.04
    steps:
      - name: Check out repo
        uses: actions/checkout@v3
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.1.42
      - name: Install dependencies
        run: bun install
      - name: Check typing
        run: bun run typecheck

  test-anvil:
    runs-on: self-hosted
    concurrency:
      group: global-self-hosted-runner
      cancel-in-progress: false
    env:
      SFOUNDRY_ROOT: /home/azureuser/seismic/seismic-foundry
    steps:
      - name: Check out repo
        uses: actions/checkout@v3
      - name: Build anvil
        run: |
          cd "$SFOUNDRY_ROOT"
          cargo build --bin sanvil
      - name: Return to main repo
        run: cd "$GITHUB_WORKSPACE"
      - name: Create .env
        # So bun doesn't log a warning about no .env file
        run: touch .env
      - name: Install dependencies
        run: bun install
      - name: Test anvil
        run: bun viem:test:anvil

  test-devnet:
    runs-on: self-hosted
    concurrency:
      group: global-self-hosted-runner
      cancel-in-progress: false
    env:
      SRETH_ROOT: /home/azureuser/seismic/seismic-reth
      RETH_DIR: /home/azureuser/.seismic-reth
      RETH_DATA_DIR: $RETH_DIR/data
      RETH_STATIC_FILES: $RETH_DIR/static_files
    steps:
      - name: Check out repo
        uses: actions/checkout@v3
      - name: Build reth
        run: |
          cd "$SRETH_ROOT"
          cargo build --bin seismic-reth
      - name: Return to main repo
        run: cd "$GITHUB_WORKSPACE"
      - name: Create .env
        # So bun doesn't log a warning about no .env file
        run: touch .env
      - name: Install dependencies
        run: bun install
      - name: Test devnet
        run: bun viem:test:devnet
      - name: Remove reth files
        run: rm -rf "$RETH_DIR"
