---
description: Pre-configured chain for connecting to the Seismic development network at node-1.seismicdev.net.
---

# seismicDevnet1

## Import

```ts
import { seismicDevnet1 } from 'seismic-viem'
```

## Usage

`seismicDevnet1` provides a pre-configured chain for connecting to the first Seismic development network node.

```ts
import { seismicDevnet1 } from 'seismic-viem'
import { createPublicClient, http } from 'viem'

const client = createPublicClient({
  chain: seismicDevnet1,
  transport: http(),
})
```

## Description

This is a pre-configured chain for connecting to the Seismic development network running at `node-1.seismicdev.net`.

- **Network Type**: Single-node development network
- **Block Explorer**: Available at `explorer-1.seismicdev.net`
- **Implementation**: Runs on Seismic's fork of reth in development mode

## Properties

The `seismicDevnet1` chain configuration contains the following properties:

### id

- **Type:** `number`
- **Value:** `5124`

The chain identifier.

### name

- **Type:** `string`
- **Value:** `"Seismic"`

Human-readable name of the chain.

### nativeCurrency

- **Type:** `object`
- **Value:** `{ decimals: 18, name: "Ether", symbol: "ETH" }`

The native currency configuration of the chain.

### rpcUrls

- **Type:** `object`
- **Value:**
  ```ts
  {
    default: {
      http: ["https://node-1.seismicdev.net/rpc"],
      webSocket: ["wss://node-1.seismicdev.net/ws"]
    }
  }
  ```

Collection of RPC endpoints for connecting to the local Anvil instance.

### formatters

- **Type:** `ChainFormatters`
- **Value:** `seismicChainFormatters`

Custom formatters for Seismic-specific data structures.
