[**seismic-viem**](/seismic-viem/README.mdx)

---

[seismic-viem](/seismic-viem/globals.mdx) / ShieldedWalletActions

# Type Alias: ShieldedWalletActions\<TChain, TAccount\>

```ts
type ShieldedWalletActions<TChain, TAccount> = object
```

Defined in: [packages/seismic-viem/src/actions/wallet.ts:45](https://github.com/SeismicSystems/seismic-client/blob/fd3f57eb71a85bad7c1f28a6e6fc7d8fcbce7480/packages/seismic-viem/src/actions/wallet.ts#L45)

Defines the actions available for a shielded wallet client.

These actions provide functionality for interacting with shielded contracts,
making signed calls, sending shielded transactions, and retrieving encryption keys.

## Type Parameters

• **TChain** _extends_ [`Chain`](https://github.com/wevm/viem) \| `undefined` = [`Chain`](https://github.com/wevm/viem) \| `undefined`

The type of the blockchain chain (extends `Chain` or `undefined`).

• **TAccount** _extends_ [`Account`](https://github.com/wevm/viem) \| `undefined` = [`Account`](https://github.com/wevm/viem) \| `undefined`

The type of the account (extends `Account` or `undefined`).

## Type declaration

### getEncryption()

```ts
;() => Hex
```

### readContract

```ts
readContract: SignedReadContract
```

### sendShieldedTransaction()

```ts
;<request, TChainOverride>(args) => Promise<SendSeismicTransactionReturnType>
```

### signedCall

```ts
signedCall: SignedCall<TChain>
```

### writeContract

```ts
writeContract: ShieldedWriteContract<TChain, TAccount>
```

## Param

The parameters required for sending the transaction.

## Returns

A promise that resolves to the result of the shielded transaction.

## Returns

The encryption key in hexadecimal format.
