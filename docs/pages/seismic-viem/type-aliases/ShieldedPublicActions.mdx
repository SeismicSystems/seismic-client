[**seismic-viem**](/seismic-viem/README.mdx)

***

[seismic-viem](/seismic-viem/globals.mdx) / ShieldedPublicActions

# Type Alias: ShieldedPublicActions\<TChain\>

> **ShieldedPublicActions**\<`TChain`\>: `object`

Defined in: [packages/seismic-viem/src/actions/public.ts:31](https://github.com/SeismicSystems/seismic-client/blob/a4d19b5c0f021acba98fd1a07111747e3fc472b0/packages/seismic-viem/src/actions/public.ts#L31)

Defines the additional actions available on a shielded public client.

These actions provide functionality specific to shielded interactions,
such as retrieving the TEE public key or interacting with storage and transactions.

## Type Parameters

• **TChain** *extends* `Chain` \| `undefined` = `Chain` \| `undefined`

## Type declaration

### getStorageAt()

> **getStorageAt**: (`args`) => `Promise`\<`GetStorageAtReturnType`\>

#### Parameters

##### args

`GetStorageAtParameters`

#### Returns

`Promise`\<`GetStorageAtReturnType`\>

### getTeePublicKey()

> **getTeePublicKey**: () => `Promise`\<`Hex` \| `string`\>

#### Returns

`Promise`\<`Hex` \| `string`\>

### getTransaction()

> **getTransaction**: (`args`) => `Promise`\<`GetTransactionReturnType`\<`TChain`, `BlockTag`\>\>

#### Parameters

##### args

`GetTransactionParameters`

#### Returns

`Promise`\<`GetTransactionReturnType`\<`TChain`, `BlockTag`\>\>

## Returns

A promise that resolves to the public key in hexadecimal or string format.
