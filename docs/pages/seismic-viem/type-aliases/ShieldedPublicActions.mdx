[**seismic-viem**](/seismic-viem/README.mdx)

---

[seismic-viem](/seismic-viem/globals.mdx) / ShieldedPublicActions

# Type Alias: ShieldedPublicActions\<TChain\>

```ts
type ShieldedPublicActions<TChain> = object
```

Defined in: [packages/seismic-viem/src/actions/public.ts:33](https://github.com/SeismicSystems/seismic-client/blob/fd3f57eb71a85bad7c1f28a6e6fc7d8fcbce7480/packages/seismic-viem/src/actions/public.ts#L33)

Defines the additional actions available on a shielded public client.

These actions provide functionality specific to shielded interactions,
such as retrieving the TEE public key or interacting with storage and transactions.

## Type Parameters

• **TChain** _extends_ [`Chain`](https://github.com/wevm/viem) \| `undefined` = [`Chain`](https://github.com/wevm/viem) \| `undefined`

The type of the blockchain chain (extends `Chain` or `undefined`).

## Type declaration

### getStorageAt()

```ts
;(args) => Promise<GetStorageAtReturnType>
```

### getTeePublicKey()

```ts
;() => Promise<Hex | string>
```

### getTransaction()

```ts
;(args) => Promise<GetTransactionReturnType<TChain, BlockTag>>
```

## Returns

A promise that resolves to the public key in hexadecimal or string format.
