[**seismic-viem**](/seismic-viem/README.mdx)

***

[seismic-viem](/seismic-viem/globals.mdx) / ShieldedWalletClient

# Type Alias: ShieldedWalletClient\<transport, chain, account\>

```ts
type ShieldedWalletClient<transport, chain, account> = Client<transport, chain, account, RpcSchema, PublicActions<transport, chain, account> & WalletActions<chain, account> & ShieldedWalletActions<chain, account>>;
```

Defined in: [packages/seismic-viem/src/client.ts:119](https://github.com/SeismicSystems/seismic-client/blob/fb2b70a799e0a58e95478a9bc32d3743c2d88729/packages/seismic-viem/src/client.ts#L119)

Represents a shielded wallet client with extended functionality for interacting
with shielded contracts, performing wallet operations, and executing public actions.

## Type Parameters

• **transport** *extends* `Transport` = `Transport`

The transport mechanism used for communication (extends `Transport`).

• **chain** *extends* `Chain` \| `undefined` = `Chain` \| `undefined`

The blockchain chain configuration (extends `Chain` or `undefined`).

• **account** *extends* `Account` \| `undefined` = `Account` \| `undefined`

The account type associated with the wallet client (extends `Account` or `undefined`).

## Remarks

The `ShieldedWalletClient` extends the base `Client` type by combining public, wallet, and shielded-specific
actions into a single client. It is designed for secure and advanced blockchain interactions that require
encryption and shielded transaction capabilities.

## Example

```typescript
const client: ShieldedWalletClient<HttpTransport, SeismicChain, LocalAccount> = createShieldedWalletClient({
  chain: seismicChain,
  transport: httpTransport,
  privateKey: '0xabcdef...',
});

// Access wallet-specific and shielded actions
const writeResult = await client.writeContract({
  address: '0x1234...',
  data: '0xdeadbeef...',
});
const encryptionKey = client.getEncryption();
console.log('Encryption Key:', encryptionKey);
```
