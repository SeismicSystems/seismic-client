[**seismic-viem**](/seismic-viem/README.mdx)

---

[seismic-viem](/seismic-viem/globals.mdx) / secp256k1Sig

# Function: secp256k1Sig()

```ts
function secp256k1Sig(client, args): Promise<Signature>
```

Defined in: [packages/seismic-viem/src/precompiles/secp256k1.ts:74](https://github.com/SeismicSystems/seismic-client/blob/9fcfa5c0984a89e84539dc512bd70bb63c77e3ef/packages/seismic-viem/src/precompiles/secp256k1.ts#L74)

Signs a message using secp256k1.

## Example

```typescript
const signature = await secp256k1Sig(client, {
  sk: '0x123...', // Your private key
  message: 'Hello, world!',
})
```

## Parameters

### client

[`CallClient`](/seismic-viem/type-aliases/CallClient.mdx)

The public client to use for the precompile call.

### args

[`Secp256K1SigParams`](/seismic-viem/type-aliases/Secp256K1SigParams.mdx)

The parameters for the secp256k1 signature operation.

- `sk` {Hex} - The secret key in hexadecimal format to use for signing.
- `message` {string} - The message to sign.

## Returns

`Promise`\<[`Signature`](https://github.com/wevm/viem)\>

A promise that resolves to the signature in the format defined by the Signature type.

## Throws

May throw if the precompile call fails.
